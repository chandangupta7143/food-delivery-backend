package com.fooddelivery.common.enums;

/**
 * System-wide user roles.
 * Used across authentication, authorization, and domain logic.
 */
public enum Role {
    ADMIN,
    USER,
    DELIVERY_PARTNER
}
